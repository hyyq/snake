#include "input.h"

uint8_t get_key(void)
{
    uint32_t ch;
    /* delay(speed);
     * if(kbhit())
     * ch=getch();
     * if(ch==PAUSE)
     * return PAUSE;
     * else if(ch==EXIT)
     * return EXIT;
     * else if(head.direc==LEFT||head.direc==RIGHT)
     * {
     * if(ch=UP||ch==DOWN)
     * return ch;
     * }
     * else if(ch==LEFT||ch==RIGHT)
     * return ch;
     * return head.direc;
     * */
}
